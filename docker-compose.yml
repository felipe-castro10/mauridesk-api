#docker run --name api-solid-pg -e POSTGRESQL_USERNAME=docker -e POSTGRESQL_PASSWORD=docker -e POSTGRESQL_DATABASE=apisolid -p 5432:5432 -d bitnami/postgresql:latest


version: '3'

services: 
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 7000:5432
    environment: 
      - POSTGRESQL_USERNAME=mauridesk 
      - POSTGRESQL_PASSWORD=m81912a
      - POSTGRESQL_DATABASE=apimauridesk